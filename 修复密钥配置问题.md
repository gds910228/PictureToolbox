# 🔧 密钥配置问题修复指南

## ✅ 正确的环境变量配置

**重要：微信云函数不支持 `TENCENTCLOUD_` 前缀的环境变量名！**

### 步骤1：在云开发控制台配置环境变量

1. **打开云开发控制台**
   - 在微信开发者工具中：云开发 → 云函数

2. **找到需要配置的云函数**（如 aiMatting、aiStyleTransfer）

3. **点击"配置" → "环境变量"**

4. **添加以下环境变量**（注意：不要用 TENCENTCLOUD_ 前缀）：

```
SECRET_ID = 你的SecretId
SECRET_KEY = 你的SecretKey
API_REGION = ap-guangzhou
```

5. **保存并重新部署云函数**

---

## 📝 环境变量名称对照表

| ❌ 错误名称（不允许使用） | ✅ 正确名称 |
|---|---|
| TENCENTCLOUD_SECRET_ID | SECRET_ID |
| TENCENTCLOUD_SECRET_KEY | SECRET_KEY |
| TENCENTCLOUD_REGION | API_REGION |

---

## 🔧 config.json 的 env 字段不会自动生效

**常见错误：**

```json
// ⚠️ 这样配置不会生效！
{
  "env": {
    "SECRET_ID": "xxx",
    "SECRET_KEY": "xxx"
  }
}
```

`config.json` 的 `env` 字段只是一个说明文档，云函数不会自动读取为环境变量。

**正确做法：**
在**云开发控制台**手动添加环境变量。

---

## 🚀 重新部署云函数

配置完环境变量后，必须重新部署：

```
右键 cloudfunctions/aiMatting 文件夹
→ 上传并部署：云端安装依赖（忽略缓存）
→ 等待1-2分钟
```

对每个需要API密钥的云函数都执行以上操作。

---

## 🧪 验证配置成功

1. **查看云函数日志**
   - 云开发控制台 → 云函数 → 选择函数 → 日志
   - 查看是否有"未配置API密钥"错误

2. **测试功能**
   - AI智能抠图
   - AI风格迁移
   - 其他需要API的功能

---

## ⚠️ 常见错误

### 错误1：环境变量Key包含前缀

```
Error: InvalidParameterValue.Environment,
环境变量Key包含前缀SCF_、QCLOUD_或TENCENTCLOUD_
```

**解决：** 使用 `SECRET_ID`、`SECRET_KEY`、`API_REGION`，不要带前缀。

### 错误2：未配置API密钥

```
未配置腾讯云API密钥，请在云函数环境变量中配置 SECRET_ID 和 SECRET_KEY
```

**解决：**
1. 确认在云开发控制台配置了环境变量
2. 确认变量名是 `SECRET_ID` 和 `SECRET_KEY`（大写，无前缀）
3. 重新部署云函数

---

## 💡 安全提示

**不要在以下地方明文存储密钥：**
- ❌ 聊天记录
- ❌ 代码文件
- ❌ Git仓库
- ❌ config.json

**正确的做法：**
- ✅ 在云开发控制台配置环境变量
- ✅ 使用 `.gitignore` 忽略敏感文件
- ✅ 定期更换密钥

---

## 📋 需要配置的云函数

以下云函数需要配置环境变量：

1. **aiMatting** - AI智能抠图
2. **aiStyleTransfer** - AI风格迁移
3. **aiDescribe** - AI图片描述
4. **aiCaption** - AI智能配文

**每个云函数都需要单独配置环境变量！**

---

**现在就按照正确的步骤配置吧！** 🚀
