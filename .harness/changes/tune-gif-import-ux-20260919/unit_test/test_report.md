# 验证用例清单 — tune-gif-import-ux-20260919

> 本项目无测试框架,验证 = 真机预览逐条执行 + 证据(Console/截图)。
> 阶段6评审:用例已并入编码评审员(code_review_v1.md §五)的补充建议,覆盖度由其复核。
> 前置:微信开发者工具打开项目根 → 预览 → 真机扫码。测试前在真机准备:一个真实 .gif 文件 + 文件传输助手。

## A. 聊天文件主路径(核心回归,2 用例)

**A1. gifEditor 按指引导入成功**
- 场景:线上"无记录"问题的正路验证
- 操作:文件传输助手聊天 + → 文件 → 选 .gif(显示为文件卡片)→ gifEditor 页点空态卡片 → 会话列表选文件传输助手 → 勾选 .gif
- 预期:选择器能看到该文件;解码进度正常 → 帧列表出现
- 证据:真机截图(帧列表页)

**A2. gifBatch 同路径多选**
- 操作:同一入口在 gifBatch 选 2 个 .gif
- 预期:两个文件卡片出现并逐个解码为"就绪"
- 证据:截图(队列页)

## B. 相册辅助入口(平台行为验证,4 用例)

**B1. 相册选 GIF(多数环境预期转码)**
- 操作:gifEditor 点"从相册试试" → 相册选一张动图
- 预期(转码环境):弹"不是 GIF 文件"modal,文案为相册版本("通常会被微信转成静态图…再勾选…从聊天导入"),非静默、非崩溃
- 预期(直出环境,若存在):正常解码进入帧列表——两分支都算过,记录实际走哪支
- 证据:modal 截图 + Console 无 error

**B2. gifBatch 相册来源失败文案**
- 操作:gifBatch 点"从相册试试" → 选动图(转码环境)
- 预期:文件卡片 status=failed,错误文案"非 GIF:相册图通常被微信转成静态 JPG,请从聊天导入";卡片可移除
- 证据:截图

**B3. 误选静态图归因(评审发现2回归)**
- 操作:gifEditor "从相册试试" → 选一张本来就是静态的 JPG/PNG
- 预期:弹相册版文案("通常会被…"),不把锅绝对甩给微信转码;流程不崩溃
- 证据:截图

**B4. gifBatch 上限边界**
- 操作:先从聊天导入 8 个 .gif → 点"从相册试试"
- 预期:相册只允许再选 1 个;导入第 9 个后 add-btn/source-actions/hint 三者消失,计数 9/9
- 证据:截图(9/9 状态)

## C. 指引与状态卫生(3 用例)

**C1. 导入指引弹窗**
- 操作:两页分别点"导入指引"
- 预期:modal 四要素齐全(文件传输助手/+→文件/图片表情不可见/iOS 存储到文件);\n 换行在 iOS、Android 各渲染一次
- 证据:双端截图

**C2. 草稿恢复来源复位(评审发现1回归)**
- 操作:gifEditor 先点一次"从相册试试"并取消 → 造一个草稿(加载 GIF 做一处编辑退出)→ 重进 → 点"恢复" 
- 预期:草稿恢复正常;即便恢复失败也不弹相册版错误文案
- 证据:截图或操作录屏

**C3. 取消路径无副作用**
- 操作:两页所有选择器(聊天/相册)直接点取消
- 预期:无 toast、无状态变化、Console 无 error
- 证据:Console 日志

## D. 回归不破坏(2 用例)

**D1. 原聊天导入路径参数未变**
- 操作:代码层确认(已由评审确认 type:'file'+extension 未动);真机重复 A1 即覆盖
- 预期:与上一版本行为一致

**D2. DevTools 编译**
- 操作:微信开发者工具重新编译
- 预期:0 error 0 warning(新增部分),两页 UI 正常渲染(source-actions 两按钮等分、单行)
- 证据:编译面板截图

## E. v2 追加用例(type:'all' 放宽,2026-09-19)

**E1. 图片气泡直发 GIF 可见性(核心)**
- 场景:线上"无结果"主投诉场景
- 操作:相册里的 GIF 直接以图片形式发到文件传输助手(不选"文件")→ gifEditor/gifBatch 点"从聊天导入" → 选该聊天
- 预期:列表**能看到**该 GIF;选中后走 GIF8 校验——若真机保留动图字节 → 正常解码(记录机型!);若转静态 → 报"以「文件」发送"引导
- 证据:列表截图 + 结果截图 + 机型/微信版本

**E2. 文件卡片路径回归**
- 操作:+ → 文件 发 .gif(白卡片)→ 同路径导入
- 预期:可见且正常解码(v1 行为不回退)
- 证据:截图

**E3. PC 端双形式**
- 操作:PC 微信分别以"拖拽发送"(图片消息)和"发送文件"(文件消息)发 GIF → 小程序(PC 端)导入
- 预期:两种形式均在列表可见;文件消息形式可正常解码,拖拽形式记录是否保留动图
- 证据:截图

## 通过标准

B1 记录实际分支、E1/E3 记录"图片形式是否保留动图"后,其余用例全过 = 验证通过;任何 Console error 或崩溃 = 失败回阶段3。
