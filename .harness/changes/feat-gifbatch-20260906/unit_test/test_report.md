# 验证用例与结果：GIF 工具链收官

- 日期：2026-09-06

## 自动化测试（已执行）

### 1. 解码器单元测试（87）
```
node scripts/gif-decoder.test.js → PASS: 87 / FAIL: 0
```

### 2. 进阶编辑测试（74）
```
node scripts/gif-adv-test.js → PASS: 74 / FAIL: 0
```

### 3. 自适应调色板测试（51）
```
node scripts/gif-adaptive-test.js → PASS: 51 / FAIL: 0
```

### 4. 引擎综合测试（31 断言 + Fuzz D）
```
node scripts/gif-engine-test.js 500
→ [1] 压缩: 9 组数据表达标
→ [2] 特效: glow/glitch/vignette/fade 全部正确
→ [3] Pipeline 端到端: 裁剪+擦除+暗角+调速+倒放+删帧 → 导出 → 再解码断言通过
→ [4] 压缩+特效 pipeline 通过
→ [5] Fuzz D: 500 pass / 0 fail（随机编辑操作序列）
→ 额外种子 42, 999: 各 200 pass / 0 fail
```

### 5. Fuzz A/B/C 回归（3 新种子 × 200 × 3 类）
```
seed=88888:  600 PASS / 0 FAIL
seed=99999:  600 PASS / 0 FAIL
seed=123456: 600 PASS / 0 FAIL
```

### 6. 分块解码验证
- decodeGifChunked 与 decodeGif 输出像素级一致（20 帧测试）
- 进度回调正确（4/20, 8/20, ... 20/20）
- 取消功能正常（3 批后取消，reject "已取消"）

### 7. 压缩数据表
9 组（3 样本 × 3 目标）全部达标或返回最优，见 coding_report。

### 8. 语法检查
7 个 JS 文件 node -c 全部通过。

---

## 手动验证用例（真机）

### V1：批量选择
- 操作：批量处理页 → 选择多个 GIF（3-9 个）
- 预期：队列显示文件名/大小/帧数/状态，逐个解码
- 证据：状态从"解码中"→"就绪"

### V2：批量调速+倒放
- 操作：选 2x 速度 + 倒放 → 开始处理
- 预期：输出 GIF 帧序反转、延时减半
- 证据：播放验证

### V3：批量压缩
- 操作：设目标 1MB → 开始处理
- 预期：每个文件压缩到 ≤1MB 或接近
- 证据：输出体积显示

### V4：批量裁剪
- 操作：选 1:1 裁剪 → 处理
- 预期：输出为正方形
- 证据：尺寸显示

### V5：单文件重试
- 操作：选择非 GIF 文件 → 失败 → 重试
- 预期：失败状态可重试

### V6：批量保存
- 操作：处理完成 → 全部保存到相册
- 预期：相册出现多个 GIF

### V7：压缩模式（编辑器）
- 操作：GIF 编辑器 → 开启目标压缩 → 设 0.5MB → 导出
- 预期：导出文件 ≤0.5MB 或接近
- 证据：结果文件大小

### V8：超大 GIF 流式解码
- 操作：选择 >150 帧 GIF
- 预期：分块解码，进度条递增，不卡死
- 证据：进度显示"解码帧 X/Y"

### V9：帧特效
- 操作：编辑器添加暗角/发光/故障特效
- 预期：预览可见特效，导出生效
- 证据：导出 GIF 播放

### V10：交叉引流
- 操作：批量页/编辑器底部 → 点"前往 GIF制作"
- 预期：正确跳转

### V11：回归-makeGif
- 操作：GIF 制作正常使用
- 预期：功能不变

### V12：回归-编辑器全部功能
- 操作：裁剪/文字/擦除/删除/截取/倒放/调速/拆帧
- 预期：全部正常
