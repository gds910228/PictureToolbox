/* ===================================
   CYBERPUNK HOMEPAGE
   =================================== */

.container {
  min-height: 100vh;
  background: transparent;
  padding: 0;
  position: relative;
  overflow: hidden;
}

/* 装饰性背景元素 */
.container::before {
  content: '';
  position: absolute;
  top: -200rpx;
  right: -200rpx;
  width: 600rpx;
  height: 600rpx;
  background: radial-gradient(circle, rgba(0, 240, 255, 0.15) 0%, transparent 70%);
  border-radius: 50%;
  filter: blur(60rpx);
  z-index: 0;
}

.container::after {
  content: '';
  position: absolute;
  bottom: -300rpx;
  left: -200rpx;
  width: 700rpx;
  height: 700rpx;
  background: radial-gradient(circle, rgba(255, 0, 128, 0.1) 0%, transparent 70%);
  border-radius: 50%;
  filter: blur(80rpx);
  z-index: 0;
}

/* ===================================
   GLASS CARD - 玻璃拟态基础类
   =================================== */

.glass-card {
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.18);
  box-shadow:
    0 8rpx 32rpx rgba(0, 0, 0, 0.25),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-lg);
}

/* ===================================
   BANNER - 顶部渐变 + 打字机
   =================================== */

.banner {
  position: relative;
  margin: 0 24rpx 40rpx;
  border-radius: var(--radius-xl);
  overflow: hidden;
  padding: 60rpx 40rpx 56rpx;
  z-index: 1;
  isolation: isolate;
  background: linear-gradient(135deg, #0A0E27 0%, #1A1F3A 100%);
  box-shadow: 0 16rpx 48rpx rgba(0, 0, 0, 0.4);
  border: 1rpx solid rgba(0, 240, 255, 0.15);
}

/* 渐变光带 */
.banner-bg {
  position: absolute;
  inset: 0;
  background:
    linear-gradient(135deg, rgba(0, 240, 255, 0.25) 0%, transparent 50%, rgba(255, 0, 128, 0.25) 100%);
  z-index: -2;
  animation: bannerShift 8s ease-in-out infinite alternate;
}

@keyframes bannerShift {
  0%   { filter: hue-rotate(0deg); opacity: 0.85; }
  100% { filter: hue-rotate(20deg); opacity: 1; }
}

/* 装饰光晕 */
.banner-glow {
  position: absolute;
  border-radius: 50%;
  filter: blur(40rpx);
  z-index: -1;
  pointer-events: none;
}

.banner-glow--cyan {
  width: 280rpx;
  height: 280rpx;
  top: -80rpx;
  left: -60rpx;
  background: radial-gradient(circle, rgba(0, 240, 255, 0.55) 0%, transparent 70%);
  animation: floatA 6s ease-in-out infinite;
}

.banner-glow--magenta {
  width: 320rpx;
  height: 320rpx;
  bottom: -120rpx;
  right: -80rpx;
  background: radial-gradient(circle, rgba(255, 0, 128, 0.45) 0%, transparent 70%);
  animation: floatB 7s ease-in-out infinite;
}

@keyframes floatA {
  0%, 100% { transform: translate(0, 0) scale(1); }
  50%      { transform: translate(20rpx, 16rpx) scale(1.08); }
}

@keyframes floatB {
  0%, 100% { transform: translate(0, 0) scale(1); }
  50%      { transform: translate(-24rpx, -20rpx) scale(1.1); }
}

.banner-content {
  position: relative;
  z-index: 1;
  text-align: left;
}

.banner-tag {
  display: inline-block;
  font-size: 22rpx;
  letter-spacing: 4rpx;
  color: var(--color-neon-cyan);
  background: rgba(0, 240, 255, 0.12);
  border: 1rpx solid rgba(0, 240, 255, 0.35);
  padding: 6rpx 18rpx;
  border-radius: 999rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 0 12rpx rgba(0, 240, 255, 0.25);
  animation: tagPulse 3s ease-in-out infinite;
}

@keyframes tagPulse {
  0%, 100% { box-shadow: 0 0 12rpx rgba(0, 240, 255, 0.25); }
  50%      { box-shadow: 0 0 20rpx rgba(0, 240, 255, 0.45); }
}

.banner-title {
  display: flex;
  align-items: baseline;
  flex-wrap: wrap;
  font-size: 56rpx;
  font-weight: 900;
  line-height: 1.25;
  color: var(--color-white);
  letter-spacing: 2rpx;
  text-shadow: 0 0 20rpx rgba(0, 240, 255, 0.35);
  margin-bottom: 16rpx;
}

.banner-title-static {
  margin-right: 12rpx;
}

.banner-title-typed {
  background: var(--gradient-neon);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  color: transparent;
  min-width: 4rpx;
}

.banner-cursor {
  display: inline-block;
  margin-left: 4rpx;
  font-weight: 400;
  color: var(--color-neon-cyan);
  -webkit-text-fill-color: var(--color-neon-cyan);
  animation: cursorBlinkBanner 1s steps(2, end) infinite;
}

@keyframes cursorBlinkBanner {
  0%, 50%   { opacity: 1; }
  51%, 100% { opacity: 0; }
}

.banner-sub {
  font-size: 24rpx;
  color: var(--color-gray-200);
  letter-spacing: 2rpx;
  opacity: 0.85;
  margin-bottom: 28rpx;
}

.banner-dots {
  display: flex;
  gap: 12rpx;
}

.banner-dot {
  width: 16rpx;
  height: 6rpx;
  border-radius: 3rpx;
  background: rgba(255, 255, 255, 0.25);
  transition: all 0.3s ease;
}

.banner-dot.is-active {
  width: 40rpx;
  background: var(--gradient-neon);
  box-shadow: 0 0 10rpx rgba(0, 240, 255, 0.6);
}

/* ===================================
   GROUPS - 折叠面板
   =================================== */

.groups {
  position: relative;
  z-index: 1;
  padding: 0 24rpx 40rpx;
}

.group {
  margin-bottom: 24rpx;
}

.group-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx 32rpx;
  border-radius: var(--radius-lg);
  transition: all 0.3s ease;
}

.group-header:active {
  transform: scale(0.99);
  background: rgba(255, 255, 255, 0.12);
}

.group-header-left {
  display: flex;
  align-items: center;
  flex: 1;
  min-width: 0;
}

.group-icon {
  width: 72rpx;
  height: 72rpx;
  border-radius: 20rpx;
  margin-right: 24rpx;
  position: relative;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0, 240, 255, 0.12);
  border: 1rpx solid rgba(0, 240, 255, 0.3);
  box-shadow: inset 0 0 12rpx rgba(0, 240, 255, 0.15);
}

.group-icon--ai {
  background: linear-gradient(135deg, rgba(0, 240, 255, 0.25) 0%, rgba(255, 0, 128, 0.2) 100%);
  border-color: rgba(255, 0, 128, 0.4);
  box-shadow: inset 0 0 16rpx rgba(255, 0, 128, 0.2), 0 0 16rpx rgba(0, 240, 255, 0.15);
}

.group-icon--basic {
  background: linear-gradient(135deg, rgba(0, 240, 255, 0.2) 0%, rgba(0, 240, 255, 0.05) 100%);
  border-color: rgba(0, 240, 255, 0.4);
}

.group-icon--creative {
  background: linear-gradient(135deg, rgba(255, 0, 128, 0.2) 0%, rgba(255, 107, 53, 0.15) 100%);
  border-color: rgba(255, 107, 53, 0.4);
}

/* AI 图标：四角星（双色光晕旋转） */
.group-icon--ai::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  margin: -3rpx 0 0 -15rpx;
  width: 30rpx;
  height: 6rpx;
  background: linear-gradient(to right, transparent 0%, var(--color-neon-cyan) 50%, transparent 100%);
  filter: drop-shadow(0 0 5rpx rgba(0, 240, 255, 0.85));
  animation: aiSparkleSpin 4s linear infinite;
}

.group-icon--ai::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  margin: -15rpx 0 0 -3rpx;
  width: 6rpx;
  height: 30rpx;
  background: linear-gradient(to bottom, transparent 0%, var(--color-hot-magenta) 50%, transparent 100%);
  filter: drop-shadow(0 0 5rpx rgba(255, 0, 128, 0.85));
  animation: aiSparkleSpin 4s linear infinite;
}

@keyframes aiSparkleSpin {
  0%   { transform: rotate(0deg)   scale(1);    }
  50%  { transform: rotate(180deg) scale(1.15); }
  100% { transform: rotate(360deg) scale(1);    }
}

/* 基础图标：齿轮 */
.group-icon--basic::before {
  content: '';
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  background:
    radial-gradient(circle, rgba(0, 240, 255, 0.45) 0%, rgba(0, 240, 255, 0.12) 38%, transparent 42%),
    repeating-conic-gradient(var(--color-neon-cyan) 0deg 14deg, transparent 14deg 45deg);
  box-shadow: 0 0 10rpx rgba(0, 240, 255, 0.45);
  animation: rotate 8s linear infinite;
}

.group-icon--basic::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  margin: -5rpx 0 0 -5rpx;
  width: 10rpx;
  height: 10rpx;
  border-radius: 50%;
  background: var(--color-neon-cyan);
  box-shadow: 0 0 8rpx var(--color-neon-cyan);
}

/* 创意图标：魔法棒 */
.group-icon--creative::before {
  content: '';
  width: 6rpx;
  height: 34rpx;
  border-radius: 3rpx;
  background: linear-gradient(to top, var(--color-electric-purple) 0%, var(--color-hot-magenta) 55%, var(--color-solar-flare) 100%);
  transform: rotate(45deg);
  box-shadow: 0 0 8rpx rgba(255, 107, 53, 0.6);
  animation: wandGlow 2s ease-in-out infinite;
}

.group-icon--creative::after {
  content: '';
  position: absolute;
  width: 6rpx;
  height: 6rpx;
  top: 14rpx;
  right: 14rpx;
  border-radius: 50%;
  background: var(--color-solar-flare);
  box-shadow:
    0 0 6rpx var(--color-solar-flare),
    0 -7rpx 0 -1rpx var(--color-solar-flare),
    0  7rpx 0 -1rpx var(--color-solar-flare),
   -7rpx 0  0 -1rpx var(--color-solar-flare),
    7rpx 0  0 -1rpx var(--color-solar-flare);
  animation: twinkle 1.4s ease-in-out infinite;
}

@keyframes wandGlow {
  0%, 100% { box-shadow: 0 0 6rpx rgba(255, 107, 53, 0.5); }
  50%      { box-shadow: 0 0 14rpx rgba(255, 0, 128, 0.85); }
}

@keyframes twinkle {
  0%, 100% { transform: scale(1)    rotate(0deg);  opacity: 1;    }
  50%      { transform: scale(0.55) rotate(45deg); opacity: 0.65; }
}

.group-meta {
  flex: 1;
  min-width: 0;
}

.group-title {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--color-white);
  letter-spacing: 2rpx;
  margin-bottom: 6rpx;
}

.group-sub {
  font-size: 22rpx;
  color: var(--color-gray-500);
  letter-spacing: 1rpx;
}

.group-chevron {
  width: 20rpx;
  height: 20rpx;
  border-right: 3rpx solid var(--color-neon-cyan);
  border-bottom: 3rpx solid var(--color-neon-cyan);
  transform: rotate(45deg);
  transition: transform 0.3s ease, border-color 0.3s ease;
  margin-right: 6rpx;
  filter: drop-shadow(0 0 4rpx rgba(0, 240, 255, 0.6));
}

.group-chevron.is-expanded {
  transform: rotate(-135deg);
  border-color: var(--color-hot-magenta);
  filter: drop-shadow(0 0 4rpx rgba(255, 0, 128, 0.6));
}

/* 折叠面板主体（带过渡） */
.group-body {
  max-height: 0;
  overflow: hidden;
  opacity: 0;
  transition: max-height 0.45s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.35s ease;
}

.group-body.is-expanded {
  max-height: 4000rpx;
  opacity: 1;
}

/* ===================================
   TOOLS GRID
   =================================== */

.tools-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
  padding: 20rpx 0 4rpx;
}

.tool-card {
  /* 玻璃拟态 */
  background: rgba(255, 255, 255, 0.06);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border-radius: var(--radius-lg);
  padding: 40rpx 24rpx 28rpx;
  text-align: center;
  border: 1rpx solid rgba(255, 255, 255, 0.14);
  box-shadow:
    0 8rpx 32rpx rgba(0, 0, 0, 0.25),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.08);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 264rpx;
  overflow: hidden;
}

/* 悬停高光 */
.tool-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, rgba(0, 240, 255, 0.12) 0%, rgba(255, 0, 128, 0.06) 100%);
  opacity: 0;
  transition: opacity 0.4s;
  z-index: 0;
}

.tool-card:active {
  transform: scale(0.96) translateY(4rpx);
  border-color: var(--color-neon-cyan);
  box-shadow:
    0 0 24rpx rgba(0, 240, 255, 0.35),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
}

.tool-card:active::before {
  opacity: 1;
}

.tool-card.disabled {
  opacity: 0.4;
  filter: grayscale(0.8);
}

/* 新功能标识样式 */
.tool-card.new-feature {
  border-color: rgba(255, 0, 128, 0.4);
  background: rgba(255, 0, 128, 0.06);
}

.new-badge {
  position: absolute;
  top: 12rpx;
  right: 12rpx;
  background: linear-gradient(135deg, #ff0080 0%, #ff8000 100%);
  color: #ffffff;
  font-size: 18rpx;
  font-weight: bold;
  padding: 4rpx 12rpx;
  border-radius: 12rpx;
  box-shadow: 0 0 12rpx rgba(255, 0, 128, 0.6);
  z-index: 2;
  animation: badgePulse 2s ease-in-out infinite;
}

@keyframes badgePulse {
  0%, 100% {
    box-shadow: 0 0 12rpx rgba(255, 0, 128, 0.6);
    transform: scale(1);
  }
  50% {
    box-shadow: 0 0 20rpx rgba(255, 0, 128, 0.9);
    transform: scale(1.05);
  }
}

/* ===================================
   CUSTOM CSS ICONS - 基础处理图标
   =================================== */

.tool-icon {
  width: 96rpx;
  height: 96rpx;
  margin-bottom: 24rpx;
  position: relative;
  z-index: 1;
}

/* Compress Icon - 外框套内块向心收缩（压缩=把图压小） */
.icon-compress {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-compress::before {
  content: '';
  position: absolute;
  width: 64rpx;
  height: 64rpx;
  border: 3rpx solid var(--color-neon-cyan);
  border-radius: 10rpx;
  box-shadow: 0 0 12rpx rgba(0, 240, 255, 0.4);
}

.icon-compress::after {
  content: '';
  position: absolute;
  width: 40rpx;
  height: 40rpx;
  background: linear-gradient(135deg, var(--color-neon-cyan) 0%, var(--color-hot-magenta) 100%);
  border-radius: 6rpx;
  box-shadow: 0 0 14rpx rgba(255, 0, 128, 0.5);
  animation: compressShrink 2s ease-in-out infinite;
}

@keyframes compressShrink {
  0%, 100% { transform: scale(1); opacity: 1; }
  50% { transform: scale(0.5); opacity: 0.7; }
}

/* Crop Icon - 裁剪框 + 四角手柄（框发光呼吸，手柄脉冲） */
.icon-crop {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-crop::before {
  content: '';
  position: absolute;
  width: 64rpx;
  height: 64rpx;
  border: 3rpx solid var(--color-neon-cyan);
  border-radius: 6rpx;
  box-shadow: 0 0 10rpx rgba(0, 240, 255, 0.3);
  animation: cropFrameGlow 2s ease-in-out infinite;
}

.icon-crop::after {
  content: '';
  position: absolute;
  width: 10rpx;
  height: 10rpx;
  background: transparent;
  border-radius: 2rpx;
  box-shadow:
    -28rpx -28rpx 0 0 var(--color-hot-magenta),
     28rpx -28rpx 0 0 var(--color-hot-magenta),
    -28rpx  28rpx 0 0 var(--color-hot-magenta),
     28rpx  28rpx 0 0 var(--color-hot-magenta);
  animation: handlePulse 1.8s ease-in-out infinite;
}

@keyframes cropFrameGlow {
  0%, 100% { box-shadow: 0 0 10rpx rgba(0, 240, 255, 0.3); }
  50% { box-shadow: 0 0 20rpx rgba(0, 240, 255, 0.65); }
}

@keyframes handlePulse {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 1; }
}

/* Convert Icon - Circular Arrows */
.icon-convert {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  border-radius: 50%;
  border: 3rpx solid transparent;
  border-top-color: var(--color-neon-cyan);
  border-right-color: var(--color-neon-cyan);
  box-shadow: 0 0 12rpx var(--color-neon-cyan);
  animation: rotate 2s linear infinite;
}

@keyframes rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.icon-convert::before {
  content: '';
  position: absolute;
  width: 0;
  height: 0;
  border-top: 16rpx solid transparent;
  border-bottom: 16rpx solid transparent;
  border-left: 20rpx solid var(--color-hot-magenta);
  top: 50%;
  left: 50%;
  transform: translate(-25%, -50%);
  filter: drop-shadow(0 0 6rpx var(--color-hot-magenta));
}

/* Watermark Icon - 图片 + 右下角版权印章盖章 */
.icon-watermark {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-watermark::before {
  content: '';
  position: absolute;
  width: 60rpx;
  height: 60rpx;
  border: 3rpx solid var(--color-neon-cyan);
  border-radius: 10rpx;
  background: linear-gradient(135deg, rgba(0, 240, 255, 0.12) 0%, rgba(255, 0, 128, 0.08) 100%);
  box-shadow: 0 0 12rpx rgba(0, 240, 255, 0.3);
}

.icon-watermark::after {
  content: '©';
  position: absolute;
  width: 32rpx;
  height: 32rpx;
  bottom: 14rpx;
  right: 14rpx;
  border-radius: 50%;
  background: var(--color-hot-magenta);
  color: #ffffff;
  font-size: 22rpx;
  font-weight: bold;
  line-height: 32rpx;
  text-align: center;
  box-shadow: 0 0 12rpx rgba(255, 0, 128, 0.6);
  animation: stampPress 2s ease-in-out infinite;
}

@keyframes stampPress {
  0%, 100% { transform: scale(1); box-shadow: 0 0 10rpx rgba(255, 0, 128, 0.5); }
  50% { transform: scale(1.18); box-shadow: 0 0 18rpx rgba(255, 0, 128, 0.9); }
}

/* Filter Icon - Diamond Grid */
.icon-filter {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  background: linear-gradient(45deg,
    var(--color-neon-cyan) 25%,
    transparent 25%,
    transparent 75%,
    var(--color-neon-cyan) 75%
  ),
  linear-gradient(-45deg,
    var(--color-hot-magenta) 25%,
    transparent 25%,
    transparent 75%,
    var(--color-hot-magenta) 75%
  );
  background-size: 32rpx 32rpx;
  background-position: 0 0, 16rpx 16rpx;
  box-shadow: 0 0 20rpx rgba(0, 240, 255, 0.3);
  animation: filterShift 3s ease-in-out infinite;
}

@keyframes filterShift {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}

/* Splice Icon - Linked Squares */
.icon-splice {
  width: 96rpx;
  height: 96rpx;
  position: relative;
}

.icon-splice::before {
  content: '';
  position: absolute;
  width: 36rpx;
  height: 36rpx;
  background: var(--color-neon-cyan);
  left: 8rpx;
  top: 50%;
  transform: translateY(-50%);
  box-shadow: 0 0 12rpx var(--color-neon-cyan);
}

.icon-splice::after {
  content: '';
  position: absolute;
  width: 36rpx;
  height: 36rpx;
  background: var(--color-hot-magenta);
  right: 8rpx;
  top: 50%;
  transform: translateY(-50%);
  box-shadow: 0 0 12rpx var(--color-hot-magenta);
}

/* ===================================
   AI TOOL ICONS - NEW DYNAMIC DESIGNS
   =================================== */

/* AI Describe Icon - 图片 + 右侧识别出的文字流 */
.icon-aiDescribe {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
}

.icon-aiDescribe::before {
  content: '';
  position: absolute;
  width: 36rpx;
  height: 36rpx;
  left: 12rpx;
  top: 50%;
  transform: translateY(-50%);
  border: 3rpx solid var(--color-neon-cyan);
  border-radius: 8rpx;
  background: linear-gradient(135deg, rgba(0, 240, 255, 0.2) 0%, rgba(255, 0, 128, 0.15) 100%);
  box-shadow: 0 0 12rpx rgba(0, 240, 255, 0.4);
}

.icon-aiDescribe::after {
  content: '';
  position: absolute;
  right: 10rpx;
  top: 50%;
  transform: translateY(-50%);
  width: 32rpx;
  height: 36rpx;
  background:
    linear-gradient(var(--color-neon-cyan), var(--color-neon-cyan)) 0 0 / 100% 4rpx no-repeat,
    linear-gradient(var(--color-hot-magenta), var(--color-hot-magenta)) 0 16rpx / 75% 4rpx no-repeat,
    linear-gradient(var(--color-neon-cyan), var(--color-neon-cyan)) 0 32rpx / 90% 4rpx no-repeat;
  animation: describeScan 1.8s ease-in-out infinite;
}

@keyframes describeScan {
  0%, 100% { opacity: 0.5; transform: translateY(-50%) translateX(0); }
  50% { opacity: 1; transform: translateY(-50%) translateX(4rpx); }
}

/* AI Caption Icon - 图片 + 下方生成的配文行 */
.icon-aiCaption {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-aiCaption::before {
  content: '';
  position: absolute;
  width: 44rpx;
  height: 32rpx;
  top: 18rpx;
  left: 50%;
  transform: translateX(-50%);
  border: 3rpx solid var(--color-neon-cyan);
  border-radius: 8rpx;
  background: linear-gradient(135deg, rgba(0, 240, 255, 0.15) 0%, rgba(255, 0, 128, 0.12) 100%);
  box-shadow: 0 0 12rpx rgba(0, 240, 255, 0.4);
}

.icon-aiCaption::after {
  content: '';
  position: absolute;
  bottom: 20rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 56rpx;
  height: 4rpx;
  border-radius: 2rpx;
  background: linear-gradient(90deg, var(--color-neon-cyan), var(--color-hot-magenta));
  box-shadow: 0 0 12rpx rgba(0, 240, 255, 0.6);
  animation: captionType 1.5s ease-in-out infinite;
}

@keyframes captionType {
  0%, 100% { width: 56rpx; opacity: 1; }
  50% { width: 30rpx; opacity: 0.6; }
}

/* AI Chat Icon - 图片相框(左上青) + 对话气泡(右下粉,内含三点,呼吸缩放) */
.icon-aiChat {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-aiChat::before {
  content: '';
  position: absolute;
  width: 42rpx;
  height: 32rpx;
  top: 22rpx;
  left: 16rpx;
  border: 3rpx solid var(--color-neon-cyan);
  border-radius: 8rpx;
  background: linear-gradient(135deg, rgba(0, 240, 255, 0.2) 0%, rgba(255, 0, 128, 0.15) 100%);
  box-shadow: 0 0 12rpx rgba(0, 240, 255, 0.4);
}

.icon-aiChat::after {
  content: '';
  position: absolute;
  width: 40rpx;
  height: 32rpx;
  bottom: 20rpx;
  right: 14rpx;
  border: 3rpx solid var(--color-hot-magenta);
  border-radius: 10rpx;
  background-color: rgba(255, 0, 128, 0.08);
  background-image: radial-gradient(circle, var(--color-hot-magenta) 2.5rpx, transparent 3rpx);
  background-size: 12rpx 100%;
  background-repeat: repeat-x;
  background-position: 8rpx center;
  box-shadow: 0 0 12rpx rgba(255, 0, 128, 0.35);
  animation: chatBubble 1.8s ease-in-out infinite;
}

@keyframes chatBubble {
  0%, 100% { transform: scale(1); box-shadow: 0 0 12rpx rgba(255, 0, 128, 0.35); }
  50% { transform: scale(1.1); box-shadow: 0 0 18rpx rgba(255, 0, 128, 0.6); }
}

/* AI Matting Icon - 主体方块从虚线底板抠起浮起 */
.icon-aiMatting {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-aiMatting::before {
  content: '';
  position: absolute;
  width: 68rpx;
  height: 48rpx;
  bottom: 14rpx;
  left: 50%;
  transform: translateX(-50%);
  border: 2rpx dashed rgba(0, 240, 255, 0.5);
  border-radius: 8rpx;
  background: rgba(0, 240, 255, 0.06);
}

.icon-aiMatting::after {
  content: '';
  position: absolute;
  width: 40rpx;
  height: 40rpx;
  left: 50%;
  top: 50%;
  background: linear-gradient(135deg, var(--color-neon-cyan) 0%, var(--color-hot-magenta) 100%);
  border-radius: 8rpx;
  box-shadow: 0 6rpx 14rpx rgba(0, 0, 0, 0.4), 0 0 18rpx rgba(0, 240, 255, 0.5);
  animation: mattingLift 2s ease-in-out infinite;
}

@keyframes mattingLift {
  0%, 100% {
    transform: translate(-50%, -50%);
    box-shadow: 0 4rpx 10rpx rgba(0, 0, 0, 0.3), 0 0 14rpx rgba(0, 240, 255, 0.4);
  }
  50% {
    transform: translate(-50%, -75%);
    box-shadow: 0 12rpx 20rpx rgba(0, 0, 0, 0.45), 0 0 24rpx rgba(255, 0, 128, 0.6);
  }
}

/* ID Photo Icon - 取景框 + 人像剪影（框呼吸换底色，人像微旋 roll 校正） */
.icon-idPhoto {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}
.icon-idPhoto::before {
  content: '';
  position: absolute;
  width: 56rpx;
  height: 72rpx;
  border: 2rpx solid var(--color-neon-cyan);
  border-radius: 6rpx;
  box-shadow: 0 0 10rpx rgba(0, 240, 255, 0.35), inset 0 0 8rpx rgba(0, 240, 255, 0.12);
  animation: idPhotoFrameGlow 2.4s ease-in-out infinite;
}
.icon-idPhoto::after {
  content: '';
  position: absolute;
  width: 48rpx;
  height: 56rpx;
  background:
    radial-gradient(circle at 50% 24%, var(--color-neon-cyan) 0 10rpx, rgba(0, 240, 255, 0) 11rpx),
    radial-gradient(ellipse 20rpx 15rpx at 50% 100%, var(--color-neon-cyan) 0 72%, rgba(0, 240, 255, 0) 73%);
  filter: drop-shadow(0 0 6rpx rgba(255, 0, 128, 0.65));
  animation: idPhotoTilt 3s ease-in-out infinite;
}
@keyframes idPhotoFrameGlow {
  0%, 100% {
    border-color: var(--color-neon-cyan);
    box-shadow: 0 0 10rpx rgba(0, 240, 255, 0.35), inset 0 0 8rpx rgba(0, 240, 255, 0.12);
  }
  50% {
    border-color: var(--color-hot-magenta);
    box-shadow: 0 0 18rpx rgba(255, 0, 128, 0.55), inset 0 0 10rpx rgba(255, 0, 128, 0.2);
  }
}
@keyframes idPhotoTilt {
  0%, 100% { transform: rotate(-3deg); }
  50%      { transform: rotate(3deg); }
}

/* AI Style Icon - Color Palette */
.icon-aiStyle {
  width: 96rpx;
  height: 96rpx;
  position: relative;
}

.icon-aiStyle::before {
  content: '';
  position: absolute;
  width: 72rpx;
  height: 72rpx;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: conic-gradient(
    from 0deg,
    var(--color-neon-cyan) 0deg,
    var(--color-hot-magenta) 60deg,
    #ffff00 120deg,
    #00ff00 180deg,
    var(--color-neon-cyan) 240deg,
    var(--color-hot-magenta) 300deg,
    var(--color-neon-cyan) 360deg
  );
  border-radius: 50%;
  box-shadow: 0 0 30rpx rgba(255, 0, 128, 0.5);
  animation: colorRotate 3s linear infinite;
}

@keyframes colorRotate {
  from {
    transform: translate(-50%, -50%) rotate(0deg);
  }
  to {
    transform: translate(-50%, -50%) rotate(360deg);
  }
}

.icon-aiStyle::after {
  content: '';
  position: absolute;
  width: 30rpx;
  height: 30rpx;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: rgba(0, 0, 0, 0.3);
  border-radius: 50%;
  backdrop-filter: blur(10rpx);
  border: 2rpx solid rgba(255, 255, 255, 0.3);
}

/* AI OCR Icon - Document with text lines & magnifying glass */
.icon-aiOCR {
  width: 96rpx;
  height: 96rpx;
  position: relative;
}

.icon-aiOCR::before {
  content: '';
  position: absolute;
  width: 60rpx;
  height: 72rpx;
  top: 50%;
  left: 50%;
  transform: translate(-55%, -50%);
  background: linear-gradient(135deg, rgba(0, 240, 255, 0.2) 0%, rgba(255, 0, 128, 0.2) 100%);
  border: 2rpx solid var(--color-neon-cyan);
  border-radius: 6rpx;
  box-shadow: 0 0 15rpx rgba(0, 240, 255, 0.3);
}

.icon-aiOCR::after {
  content: '';
  position: absolute;
  width: 40rpx;
  height: 36rpx;
  top: 50%;
  left: 50%;
  transform: translate(-55%, -45%);
  background:
    linear-gradient(to right, var(--color-neon-cyan) 100%, transparent 0) 0 0 / 100% 4rpx no-repeat,
    linear-gradient(to right, var(--color-hot-magenta) 70%, transparent 0) 0 14rpx / 100% 4rpx no-repeat,
    linear-gradient(to right, var(--color-neon-cyan) 85%, transparent 0) 0 28rpx / 100% 4rpx no-repeat;
  animation: textScan 2s ease-in-out infinite;
}

@keyframes textScan {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 1; }
}

/* AI Eraser Icon - Magic eraser with sparkle effect */
.icon-aiEraser {
  width: 96rpx;
  height: 96rpx;
  position: relative;
}

.icon-aiEraser::before {
  content: '';
  position: absolute;
  width: 50rpx;
  height: 60rpx;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) rotate(-15deg);
  background: linear-gradient(180deg,
    var(--color-neon-cyan) 0%,
    var(--color-neon-cyan) 40%,
    var(--color-hot-magenta) 40%,
    var(--color-hot-magenta) 100%
  );
  border-radius: 6rpx 6rpx 12rpx 12rpx;
  box-shadow: 0 0 20rpx rgba(0, 240, 255, 0.5);
  animation: eraserFloat 2s ease-in-out infinite;
}

@keyframes eraserFloat {
  0%, 100% {
    transform: translate(-50%, -50%) rotate(-15deg);
    box-shadow: 0 0 20rpx rgba(0, 240, 255, 0.5);
  }
  50% {
    transform: translate(-50%, -55%) rotate(-12deg);
    box-shadow: 0 0 30rpx rgba(255, 0, 128, 0.6);
  }
}

.icon-aiEraser::after {
  content: '';
  position: absolute;
  width: 12rpx;
  height: 12rpx;
  top: 10rpx;
  right: 8rpx;
  background: var(--color-neon-cyan);
  border-radius: 50%;
  box-shadow:
    0 0 8rpx var(--color-neon-cyan),
    -15rpx 20rpx 0 -2rpx var(--color-hot-magenta),
    -15rpx 20rpx 6rpx -2rpx var(--color-hot-magenta),
    10rpx 30rpx 0 -3rpx var(--color-neon-cyan),
    10rpx 30rpx 6rpx -3rpx var(--color-neon-cyan);
  animation: sparklePulse 1.5s ease-in-out infinite;
}

@keyframes sparklePulse {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.6; transform: scale(0.8); }
}

/* ===================================
   TOOL CARD CONTENT
   =================================== */

.tool-name {
  font-size: 28rpx;
  font-weight: 700;
  color: var(--color-white);
  margin-bottom: 12rpx;
  letter-spacing: 1rpx;
  position: relative;
  z-index: 1;
}

.tool-desc {
  font-size: 22rpx;
  color: var(--color-gray-500);
  line-height: 1.4;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  overflow: hidden;
  text-overflow: ellipsis;
  position: relative;
  z-index: 1;
}

.tool-status {
  position: absolute;
  top: 16rpx;
  right: 16rpx;
  background: rgba(0, 240, 255, 0.1);
  color: var(--color-neon-cyan);
  font-size: 20rpx;
  padding: 6rpx 16rpx;
  border-radius: 20rpx;
  border: 1rpx solid var(--color-neon-cyan);
  backdrop-filter: blur(10rpx);
  z-index: 2;
}

/* ===================================
   FEATURES SECTION - CARDS
   =================================== */

.features {
  background: rgba(255, 255, 255, 0.04);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border-radius: var(--radius-lg);
  padding: 40rpx;
  margin: 0 24rpx 40rpx;
  border: 1rpx solid rgba(0, 240, 255, 0.15);
  position: relative;
  z-index: 1;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.2);
}

.features::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2rpx;
  background: var(--gradient-neon);
  opacity: 0.5;
}

.feature-item {
  display: flex;
  align-items: center;
  margin-bottom: 24rpx;
}

.feature-item:last-child {
  margin-bottom: 0;
}

.feature-icon {
  width: 48rpx;
  height: 48rpx;
  margin-right: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--gradient-neon);
  border-radius: 50%;
  font-size: 24rpx;
  box-shadow: var(--shadow-neon);
}

.feature-text {
  font-size: 26rpx;
  color: var(--color-gray-200);
  letter-spacing: 1rpx;
}

/* ===================================
   FOOTER
   =================================== */

.footer {
  text-align: center;
  padding: 40rpx 0 80rpx;
  position: relative;
  z-index: 1;
}

.footer-text {
  font-size: 22rpx;
  color: var(--color-gray-500);
  letter-spacing: 2rpx;
  opacity: 0.6;
}

.footer::before {
  content: '';
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 120rpx;
  height: 2rpx;
  background: var(--gradient-neon);
}

/* ===================================
   NEW TOOL ICONS: imgToPdf, exif
   =================================== */

/* PDF 文档图标：纸张 + "PDF" 字 */
.icon-imgToPdf {
  width: 96rpx;
  height: 96rpx;
  position: relative;
}
.icon-imgToPdf::before {
  content: 'PDF';
  position: absolute;
  width: 64rpx;
  height: 80rpx;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: linear-gradient(135deg, rgba(255,0,128,0.2) 0%, rgba(0,240,255,0.18) 100%);
  border: 2rpx solid var(--color-neon-cyan);
  border-radius: 8rpx;
  box-shadow: 0 0 18rpx rgba(0,240,255,0.35);
  color: #00F0FF;
  font-size: 22rpx;
  font-weight: 700;
  line-height: 80rpx;
  text-align: center;
  letter-spacing: 1rpx;
}
.icon-imgToPdf::after {
  content: '';
  position: absolute;
  width: 16rpx;
  height: 16rpx;
  top: calc(50% - 40rpx);
  left: calc(50% + 16rpx);
  background: linear-gradient(135deg, rgba(0,240,255,0.4) 0%, transparent 100%);
  border-left: 2rpx solid var(--color-neon-cyan);
  border-bottom: 2rpx solid var(--color-neon-cyan);
}

/* GIF Icon - 胶片帧（孔位滚动）+ 播放三角（呼吸辉光） */
.icon-makeGif {
  width: 96rpx;
  height: 96rpx;
  position: relative;
}
.icon-makeGif::before {
  content: '';
  position: absolute;
  width: 56rpx;
  height: 72rpx;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  border: 2rpx solid var(--color-neon-cyan);
  border-radius: 8rpx;
  background-color: rgba(10, 14, 39, 0.55);
  background-image:
    radial-gradient(circle, rgba(0, 240, 255, 0.85) 2rpx, transparent 3rpx),
    radial-gradient(circle, rgba(0, 240, 255, 0.85) 2rpx, transparent 3rpx),
    linear-gradient(135deg, rgba(255, 0, 128, 0.20) 0%, rgba(0, 240, 255, 0.14) 100%);
  background-position: 0 3rpx, 0 59rpx, 0 0;
  background-size: 10rpx 10rpx, 10rpx 10rpx, 100% 100%;
  background-repeat: repeat-x, repeat-x, no-repeat;
  box-shadow: 0 0 14rpx rgba(0, 240, 255, 0.35);
  animation: gif-film-roll 1.6s linear infinite;
}
.icon-makeGif::after {
  content: '';
  position: absolute;
  width: 0;
  height: 0;
  top: 50%;
  left: calc(50% + 3rpx);
  transform: translateY(-50%);
  border-left: 18rpx solid var(--color-hot-magenta);
  border-top: 12rpx solid transparent;
  border-bottom: 12rpx solid transparent;
  filter: drop-shadow(0 0 6rpx rgba(255, 0, 128, 0.6));
  animation: gif-play-pulse 1.6s ease-in-out infinite;
}
@keyframes gif-film-roll {
  from { background-position: 0 3rpx, 0 59rpx, 0 0; }
  to   { background-position: 10rpx 3rpx, 10rpx 59rpx, 0 0; }
}
@keyframes gif-play-pulse {
  0%, 100% {
    filter: drop-shadow(0 0 5rpx rgba(255, 0, 128, 0.55));
    opacity: 0.82;
  }
  50% {
    filter: drop-shadow(0 0 11rpx rgba(255, 0, 128, 1));
    opacity: 1;
  }
}

/* 隐形水印图标：像素网格中一颗闪烁的隐藏比特 */
.icon-hiddenWatermark {
  width: 96rpx;
  height: 96rpx;
  position: relative;
}
.icon-hiddenWatermark::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 56rpx;
  height: 56rpx;
  border: 2rpx solid var(--color-neon-cyan);
  border-radius: 6rpx;
  background:
    linear-gradient(rgba(0, 240, 255, 0.28), rgba(0, 240, 255, 0.28)) center/2rpx 100% no-repeat,
    linear-gradient(rgba(0, 240, 255, 0.28), rgba(0, 240, 255, 0.28)) center/100% 2rpx no-repeat;
  box-shadow: 0 0 12rpx rgba(0, 240, 255, 0.3);
  animation: hwGridGlow 2.6s ease-in-out infinite;
}
.icon-hiddenWatermark::after {
  content: '';
  position: absolute;
  top: calc(50% + 14rpx);
  left: calc(50% + 14rpx);
  width: 20rpx;
  height: 20rpx;
  background: var(--color-hot-magenta);
  border-radius: 3rpx;
  box-shadow: 0 0 12rpx rgba(255, 0, 128, 0.8);
  animation: hwBitBlink 1.8s ease-in-out infinite;
}
@keyframes hwGridGlow {
  0%, 100% { box-shadow: 0 0 12rpx rgba(0, 240, 255, 0.3); border-color: var(--color-neon-cyan); }
  50% { box-shadow: 0 0 18rpx rgba(0, 240, 255, 0.55); border-color: #66f5ff; }
}
@keyframes hwBitBlink {
  0%, 100% { opacity: 0.2; transform: translate(-50%, -50%) scale(0.85); }
  50% { opacity: 1; transform: translate(-50%, -50%) scale(1.1); }
}

/* EXIF 图标：放大镜 */
.icon-exif {
  width: 96rpx;
  height: 96rpx;
  position: relative;
}
.icon-exif::before {
  content: '';
  position: absolute;
  width: 48rpx;
  height: 48rpx;
  top: 50%;
  left: 50%;
  transform: translate(-58%, -58%);
  border: 4rpx solid var(--color-neon-cyan);
  border-radius: 50%;
  background: radial-gradient(circle, rgba(0,240,255,0.15) 0%, transparent 70%);
  box-shadow: 0 0 16rpx rgba(0,240,255,0.45);
}
.icon-exif::after {
  content: '';
  position: absolute;
  width: 24rpx;
  height: 4rpx;
  top: 50%;
  left: 50%;
  transform: translate(2rpx, 18rpx) rotate(45deg);
  background: var(--color-neon-cyan);
  border-radius: 2rpx;
  box-shadow: 0 0 10rpx rgba(0,240,255,0.6);
}

/* 颜色分析图标：四色色卡条 + 取色环 */
.icon-colorAnalysis {
  width: 96rpx;
  height: 96rpx;
  position: relative;
}
.icon-colorAnalysis::before {
  content: '';
  position: absolute;
  width: 60rpx;
  height: 46rpx;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  border: 2rpx solid var(--color-neon-cyan);
  border-radius: 8rpx;
  /* 四色硬切分，模拟色卡 */
  background: linear-gradient(to right,
    var(--color-neon-cyan) 0%, var(--color-neon-cyan) 25%,
    var(--color-hot-magenta) 25%, var(--color-hot-magenta) 50%,
    var(--color-electric-purple) 50%, var(--color-electric-purple) 75%,
    var(--color-solar-flare) 75%, var(--color-solar-flare) 100%);
  box-shadow: 0 0 14rpx rgba(0,240,255,0.35);
}
.icon-colorAnalysis::after {
  content: '';
  position: absolute;
  width: 20rpx;
  height: 20rpx;
  top: 50%;
  left: 50%;
  transform: translate(16rpx, 14rpx);
  border: 2rpx solid var(--color-neon-cyan);
  border-radius: 50%;
  background: rgba(10, 14, 39, 0.92);
  box-shadow: 0 0 8rpx rgba(0, 240, 255, 0.55);
}

/* 格式推荐图标：带格式头的文档 + 推荐标记（菱形） */
.icon-formatRecommend {
  width: 96rpx;
  height: 96rpx;
  position: relative;
}
.icon-formatRecommend::before {
  content: '';
  position: absolute;
  width: 44rpx;
  height: 54rpx;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  border: 2rpx solid var(--color-neon-cyan);
  border-radius: 6rpx;
  /* 顶部格式色带 + 文档体 */
  background: linear-gradient(to bottom,
    var(--color-neon-cyan) 0%, var(--color-neon-cyan) 28%,
    var(--color-deep-space) 28%, var(--color-deep-space) 100%);
  box-shadow: 0 0 14rpx rgba(0, 240, 255, 0.35);
}
.icon-formatRecommend::after {
  content: '';
  position: absolute;
  width: 16rpx;
  height: 16rpx;
  top: 50%;
  left: 50%;
  transform: translate(14rpx, 14rpx) rotate(45deg);
  background: var(--color-hot-magenta);
  box-shadow: 0 0 10rpx rgba(255, 0, 128, 0.7);
}

/* ===================================
   AI UPSCALE ICON - 中心图块呼吸放大 + 外扩波纹
   语义：原图 → AI 超分辨率放大，分辨率向外提升
   =================================== */

.icon-aiUpscale {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 中心原图块：渐变填充 + 呼吸放大（AI 增强细节） */
.icon-aiUpscale::before {
  content: '';
  position: absolute;
  width: 40rpx;
  height: 40rpx;
  top: 50%;
  left: 50%;
  border-radius: 8rpx;
  background: linear-gradient(135deg, var(--color-neon-cyan) 0%, var(--color-hot-magenta) 100%);
  transform: translate(-50%, -50%) scale(0.85);
  box-shadow: 0 0 18rpx rgba(0, 240, 255, 0.6);
  animation: upscaleCore 2s ease-in-out infinite;
  z-index: 2;
}

@keyframes upscaleCore {
  0%, 100% {
    transform: translate(-50%, -50%) scale(0.8);
    box-shadow: 0 0 18rpx rgba(0, 240, 255, 0.6);
  }
  50% {
    transform: translate(-50%, -50%) scale(1.12);
    box-shadow: 0 0 28rpx rgba(255, 0, 128, 0.75);
  }
}

/* 外扩波纹框：从中心向外扩散并淡出（放大扩散波） */
.icon-aiUpscale::after {
  content: '';
  position: absolute;
  width: 40rpx;
  height: 40rpx;
  top: 50%;
  left: 50%;
  border: 3rpx solid var(--color-neon-cyan);
  border-radius: 10rpx;
  transform: translate(-50%, -50%) scale(0.8);
  opacity: 0.9;
  box-shadow: 0 0 12rpx rgba(0, 240, 255, 0.4);
  animation: upscaleWave 2s ease-out infinite;
  z-index: 1;
}

@keyframes upscaleWave {
  0% {
    transform: translate(-50%, -50%) scale(0.8);
    opacity: 0.9;
    border-color: var(--color-neon-cyan);
  }
  70% {
    border-color: var(--color-hot-magenta);
  }
  100% {
    transform: translate(-50%, -50%) scale(2.2);
    opacity: 0;
    border-color: var(--color-hot-magenta);
  }
}

/* ============================================================
 * AI 老照片上色图标：一张「照片」从左到右被色彩铺满（上色过程）。
 *   ::before = 黑白照片底图（灰阶，上浅下深暗示天空/地面）
 *   ::after  = 彩色层，从左缘横向 scaleX 铺开 → 直观表达「黑白变彩色」
 * ============================================================ */
.icon-aiColorize {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 黑白照片底图：灰阶圆角矩形（上浅下深，暗示风景照） */
.icon-aiColorize::before {
  content: '';
  position: absolute;
  left: 21rpx;
  top: 27rpx;
  width: 54rpx;
  height: 42rpx;
  border-radius: 8rpx;
  background: linear-gradient(180deg, #6b7896 0%, #3f4860 100%);
  z-index: 1;
}

/* 彩色层：从左缘横向铺开（上色扫过），覆盖底图 */
.icon-aiColorize::after {
  content: '';
  position: absolute;
  left: 21rpx;
  top: 27rpx;
  width: 54rpx;
  height: 42rpx;
  border-radius: 8rpx;
  background: linear-gradient(180deg, var(--color-neon-cyan) 0%, #38ef7d 55%, var(--color-hot-magenta) 100%);
  transform-origin: 0 50%;
  transform: scaleX(0);
  box-shadow: 2rpx 0 10rpx rgba(0, 240, 255, 0.5);
  animation: colorizeWipe 2.8s ease-in-out infinite;
  z-index: 2;
}

@keyframes colorizeWipe {
  0%, 100% { transform: scaleX(0); }
  45%, 70% { transform: scaleX(1); }
}

/* ===================================
   SIMILARITY ICON - 两个错位方框（双图对比）
   =================================== */
.icon-similarity {
  width: 96rpx;
  height: 96rpx;
  position: relative;
}
.icon-similarity::before {
  content: '';
  position: absolute;
  width: 44rpx;
  height: 44rpx;
  left: 12rpx;
  top: 50%;
  transform: translateY(-50%);
  border: 3rpx solid var(--color-neon-cyan);
  border-radius: 8rpx;
  background: rgba(0, 240, 255, 0.12);
  box-shadow: 0 0 14rpx rgba(0, 240, 255, 0.5);
  animation: similarityPulse 2.4s ease-in-out infinite;
}
.icon-similarity::after {
  content: '';
  position: absolute;
  width: 44rpx;
  height: 44rpx;
  right: 12rpx;
  top: 50%;
  transform: translateY(-50%);
  border: 3rpx solid var(--color-hot-magenta);
  border-radius: 8rpx;
  background: rgba(255, 0, 128, 0.12);
  box-shadow: 0 0 14rpx rgba(255, 0, 128, 0.5);
}
@keyframes similarityPulse {
  0%, 100% { transform: translateY(-50%) scale(1); opacity: 1; }
  50% { transform: translateY(-50%) scale(0.92); opacity: 0.75; }
}

/* AI 扩图：外层虚线框=扩图后边界，内层渐变框=原图，呼吸式向外扩展 */
.icon-aiOutpaint {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-aiOutpaint::before {
  content: '';
  position: absolute;
  width: 64rpx;
  height: 48rpx;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  border: 2rpx dashed rgba(0, 240, 255, 0.5);
  border-radius: 8rpx;
  background: rgba(0, 240, 255, 0.06);
}

.icon-aiOutpaint::after {
  content: '';
  position: absolute;
  width: 36rpx;
  height: 28rpx;
  left: 50%;
  top: 50%;
  background: linear-gradient(135deg, var(--color-neon-cyan) 0%, var(--color-hot-magenta) 100%);
  border-radius: 6rpx;
  box-shadow: 0 0 16rpx rgba(0, 240, 255, 0.5);
  animation: outpaintExpand 2s ease-in-out infinite;
}

@keyframes outpaintExpand {
  0%, 100% { transform: translate(-50%, -50%) scale(0.62); opacity: 0.85; }
  50% { transform: translate(-50%, -50%) scale(1); opacity: 1; }
}

/* AI 文生图：左侧文字行 → 右侧渐变生成框，表达「文字描述生成图片」 */
.icon-aiTextToImage {
  width: 96rpx;
  height: 96rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 右侧渐变生成框（生成的图片） */
.icon-aiTextToImage::before {
  content: '';
  position: absolute;
  width: 44rpx;
  height: 44rpx;
  right: 14rpx;
  top: 50%;
  transform: translateY(-50%);
  border-radius: 8rpx;
  background: linear-gradient(135deg, var(--color-neon-cyan) 0%, var(--color-hot-magenta) 100%);
  box-shadow: 0 0 16rpx rgba(0, 240, 255, 0.5);
  animation: text2imgFrame 2.4s ease-in-out infinite;
}

@keyframes text2imgFrame {
  0%, 100% { box-shadow: 0 0 16rpx rgba(0, 240, 255, 0.5); }
  50% { box-shadow: 0 0 24rpx rgba(255, 0, 128, 0.7); }
}

/* 左侧三行文字（提示词），向生成框方向轻移，暗示「文字流入生成图」 */
.icon-aiTextToImage::after {
  content: '';
  position: absolute;
  left: 16rpx;
  top: 50%;
  transform: translateY(-50%);
  width: 26rpx;
  height: 22rpx;
  background: repeating-linear-gradient(
    to bottom,
    var(--color-neon-cyan) 0,
    var(--color-neon-cyan) 3rpx,
    transparent 3rpx,
    transparent 9rpx
  );
  border-radius: 2rpx;
  opacity: 0.85;
  animation: text2imgFlow 2.4s ease-in-out infinite;
}

@keyframes text2imgFlow {
  0%, 100% { transform: translateY(-50%) translateX(0); opacity: 0.85; }
  50% { transform: translateY(-50%) translateX(6rpx); opacity: 1; }
}

