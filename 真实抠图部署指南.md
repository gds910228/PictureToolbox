# 🎉 真实抠图功能部署指南

## ✅ 已完成的修改

### 云函数代码更新

**修改文件：** `cloudfunctions/aiMatting/index.js`

**关键改动：**
```javascript
// ❌ 旧代码（错误）
const IaiClient = require('tencentcloud-sdk-nodejs').iai.v20200303.Client;
await client.PortraitMatting(params);

// ✅ 新代码（正确）
const BdaClient = require('tencentcloud-sdk-nodejs').bda.v20200324.Client;
await client.SegmentPortraitPic(params);
```

**API变化：**
- 从：人脸识别服务（IAI）的 PortraitMatting API
- 到：人体分析服务（BDA）的 SegmentPortraitPic API

---

## 🚀 部署步骤

### 第1步：开通腾讯云人体分析服务

1. **登录腾讯云控制台**
   - 访问：https://console.cloud.tencent.com/

2. **搜索人体分析服务**
   - 在顶部搜索框输入：**"人体分析"** 或 **"Body Analysis"**
   - 或直接访问：https://console.cloud.tencent.com/bda

3. **开通服务**
   - 点击"开通服务"按钮
   - 选择计费方式：
     - **按量计费**（推荐，有免费额度）
     - 或购买资源包

4. **确认服务已开通**
   - 进入"人体分析"控制台
   - 确认状态为"已开通"

### 第2步：确认API密钥权限

确保你的API密钥有权限访问人体分析服务：
- SecretId: 你的SecretId（在云函数环境变量中配置）
- SecretKey: 你的SecretKey（在云函数环境变量中配置）

**注意：** API密钥应配置在云函数的环境变量中，不要硬编码在代码里。

如果需要，可以在腾讯云控制台创建新的API密钥。

### 第3步：重新部署云函数

**在微信开发者工具中：**

```
1. 右键点击 cloudfunctions/aiMatting 文件夹
2. 选择："上传并部署：云端安装依赖"
3. 勾选 "忽略缓存"（重要！）
4. 点击确定，等待部署完成（约1-2分钟）
```

**验证部署成功：**
- 部署完成后，云函数文件夹旁边会显示绿色的云朵图标 ☁️
- 控制台显示"上传并部署成功"

### 第4步：测试功能

1. **打开小程序**
   - 在微信开发者工具中点击"编译"
   - 或扫描预览二维码在真机上测试

2. **进入AI智能抠图页面**
   - 点击首页的"AI智能抠图"工具卡片

3. **上传测试图片**
   - 建议使用清晰的人像照片
   - 背景不要太复杂

4. **开始抠图**
   - 点击"开始抠图"按钮
   - 等待处理（约3-10秒）

5. **查看结果**
   - 如果看到 **"✅"** 图标 → 真实抠图成功！
   - 如果看到 **"📝"** 图标 → 智能识别模式（API调用失败）

---

## 📊 预期结果

### ✅ 成功：真实抠图

**弹窗提示：**
```
✨ 抠图成功！

真实抠图完成！

已去除背景，生成透明PNG。

当前背景：透明
```

**视觉效果：**
- 左侧：原图
- 右侧：只有人像，背景完全透明
- 标签显示：**"抠图后 ✅"**

**调试信息：**
```
调试信息：fileID: cloud://..., isRealMatting: true
当前模式：真实抠图
选择的类型：portrait
```

### ❌ 失败：智能识别模式

**弹窗提示：**
```
✨ AI智能识别成功

📷 AI识别结果：
• 主体类型：其他
• 主体描述：智能识别完成
• 背景描述：已分析
• 置信度：80%

⚠️ 提示：当前为智能识别模式（Beta版）。
```

**原因：**
- 人体分析服务未开通
- API密钥权限不足
- 账户余额不足
- 图片不符合要求

---

## 🔧 故障排查

### 如果还是显示智能识别模式

**检查清单：**

1. **确认服务已开通**
   - 登录腾讯云控制台
   - 搜索"人体分析"
   - 确认服务状态为"已开通"

2. **查看云函数日志**
   ```
   在云开发控制台：
   1. 点击"云函数"
   2. 找到 "aiMatting"
   3. 点击"日志"
   4. 查看最新的错误信息
   ```

3. **常见错误及解决方案**

   **错误1：服务未开通**
   ```
   错误信息：ResourceUnavailable.NotExist
   解决方案：开通人体分析服务
   ```

   **错误2：余额不足**
   ```
   错误信息：FailedOperation.BalanceInsufficient
   解决方案：充值账户
   ```

   **错误3：权限不足**
   ```
   错误信息：AuthFailure
   解决方案：检查API密钥权限
   ```

   **错误4：图片过大**
   ```
   错误信息：FailedOperation.ImageSizeExceed
   解决方案：使用小于5M的图片
   ```

4. **复制错误信息**
   - 如果看到错误，复制完整的错误信息
   - 发送给开发者进行诊断

---

## 💰 计费说明

### 人体分析服务定价

**计费方式：** 按量计费

**参考价格：**
- 免费额度：通常有每月免费额度（具体查看控制台）
- 超出后：约 ¥0.01-0.05/次

**成本估算：**
- 100次/月 ≈ ¥0-5
- 1000次/月 ≈ ¥0-50

**省钱技巧：**
1. 利用免费额度
2. 如果量大，购买资源包更划算
3. 智能识别模式完全免费

---

## 🎯 API文档参考

**人像分割API：**
- 文档：https://cloud.tencent.com/document/api/1208/42970
- 接口：SegmentPortraitPic
- 请求域名：bda.tencentcloudapi.com
- 支持地域：ap-beijing, ap-guangzhou, ap-shanghai, ap-singapore

**限制条件：**
- 图片大小：base64后不超过5M
- 图片分辨率：小于2000*2000
- 支持格式：PNG, JPG, JPEG, BMP
- 不支持：GIF

---

## 📝 后续优化建议

### 短期
- ✅ 添加更多抠图类型（商品、通用）
- ✅ 支持批量处理
- ✅ 优化错误提示

### 长期
- ✅ 支持视频抠图
- ✅ 添加背景更换功能
- ✅ 支持自定义分割

---

**现在开始部署吧！** 🚀

按照步骤操作，5分钟后你就能看到真实的抠图效果！

如果遇到问题，请查看云函数日志或联系开发者。
